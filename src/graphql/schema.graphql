type Query {
  allProjects: [Project]
}

type Profile {
  id: ID!
  name: String
}

type Project {
  id: ID!
  name: String
  description: String
  type: String
  key: String
  archived: Boolean
  createdAt: DateTime
  updatedAt: DateTime
  profileId: ID
  Profile: Profile
}
